# Vue 应用 Dockerfile - 知识推理系统
FROM nginx:alpine

# 复制构建产物到 nginx 目录
COPY dist-20251011-v3.2/ /usr/share/nginx/html/

# 复制 nginx 配置
COPY dist-20251011-v3.2/nginx.conf /etc/nginx/conf.d/default.conf

# 暴露端口 80
EXPOSE 80

# 健康检查
HEALTHCHECK --interval=30s --timeout=10s --retries=3 \
  CMD curl -f http://localhost/ || exit 1

# 启动 nginx
CMD ["nginx", "-g", "daemon off;"]
